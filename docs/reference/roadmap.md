# Roadmap

Completed in the current enterprise implementation:

- kernel-owned provider mediation
- external MCP stdio, HTTP, and SSE adapter execution
- dedicated authority create and inspect commands
- scenario golden corpora and eval gate
- split reference documentation
- named `runwarden-kernel` binary
- generated TypeScript contract pipeline
- WebUI responsive and accessibility contract checks

Next roadmap depth should focus on production deployment packaging, richer adapter catalogs, and larger adversarial eval suites.
