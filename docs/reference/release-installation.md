# Release Installation

Release evidence is produced by `scripts/release_gate_local.sh` and `.github/workflows/release.yml`.

Named binaries:

- `runwarden`
- `runwarden-mcp`
- `runwarden-kernel`

Release artifacts include generated schemas, submission artifacts, SBOM, provenance, cert results, bench results, agent-native eval results, and scenario golden-corpus eval results.
